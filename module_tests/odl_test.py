# This script can be used to troubleshoot problems with ODL or the environment. Students should not run these tests needlessly.
# Note that the current version (0.8.2) of ODL skips many tests and throws a lot of errors, even when working as intended.

import odl

odl.test()
